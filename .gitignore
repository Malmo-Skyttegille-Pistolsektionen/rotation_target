
# Impeccable: per-developer overrides and the hook's dedup cache
**/.impeccable/config.local.json
**/.impeccable/hook.cache.json

# MkDocs build output
/site/

# Extracted by the contracts workflow to diff a PR against the base branch.
.contracts-base/
