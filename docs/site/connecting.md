# Connecting

The board serves its own web app over WiFi — join the same network the device
is on and open its address in a browser. A device that has never joined a
network falls back to a setup portal of its own, signalled by a blue
[status LED](status-led.md).

<!-- TODO: screenshots -->
